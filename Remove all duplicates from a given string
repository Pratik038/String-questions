#include <bits/stdc++.h>
using namespace std;

int main() {
	//code
	int t; cin>>t;
	while(t--){
	    string st; cin>>st;
	    int a[256]={};
	    for(int i=0;i<st.size();i++){
	        a[st[i]]++;
	        if(a[st[i]]<2)
	            cout<<st[i];
	    }
	    cout<<endl;
	}
	return 0;
}
